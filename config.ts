export const config = {
    zoomFactor: 1.1, // factor de zoom al usar la rueda del mouse
    minScale: 10, // escala mínima
    maxScale: 500, // escala máxima
    initialScale: 100, // escala inicial del canvas
}